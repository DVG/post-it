<% if @sticky_note.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@sticky_note.errors.count, "error") %> prohibited this sticky_note from being saved:</h2>

    <ul>
    <% @sticky_note.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :content %><br>
  <%= f.text_area :content %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
